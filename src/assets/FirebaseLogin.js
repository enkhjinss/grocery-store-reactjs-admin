import {
    GoogleAuthProvider,
    signInWithPopup,
    GithubAuthProvider,
    RecaptchaVerifier,
    signInWithPhoneNumber,
} from "firebase/auth";
import auth from "../firebase";
//1 user butsaadag bn
//
export const FirebaseLogin = () => {
    return;
};
